<html>
    <body>
        <h1> Users Management Page </h1>
        <% users.forEach(function(user) {%>
            <li> <%= user.username %> 
                <a href = "/update/<%= user.username%>"> update</a>
                <a href = "/delete/<%= user.username%>"> delete</a>
            </li>
            <% })%>
        <a href= "/userDataPage"> Add New</a>
    </body>
</html>